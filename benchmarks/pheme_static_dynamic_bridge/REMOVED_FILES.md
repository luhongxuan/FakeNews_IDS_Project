# Removed files

Old training-oriented Bridge runners and repeated denominator experiments were
replaced by `run.py`, which replays policies from immutable OOF predictions.
Historical exploratory outputs are preserved under ignored `analysis/benchmarks/`;
the authoritative Bridge A result remains under `reference_result/`.
